# Description: gtkmm is the official C++ interface for the popular GUI library GTK+ (version 3)
# URL: https://gtkmm.org
# Maintainer: Tsaop, leeroy at cock dot li
# Depends on: glibmm pangomm cairomm gtk3 atkmm

name=gtkmm3
version=3.24.2
release=1
source=(https://download.gnome.org/sources/${name%3}/${version%.2}/${name%3}-$version.tar.xz)

build() {
	cd ${name%3}-$version

	./configure --prefix=/usr

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share/doc
}
