# Description:  Music playback daemon
# URL:          http://www.musicpd.org/
# Maintainer:   Tsaop, leeroy at cock dot li
# Depends on:alsa-lib audiofile boost expat faad2 ffmpeg flac glib icu lame libcdio libcdio-paranoia libid3tag libmad libogg libsamplerate libsndfile libvdpau libvorbis mpg123 opus sqlite3 wavpack xorg-libx11 xorg-libxau xorg-libxcb xorg-libxdmcp xorg-libxext

name=mpd
version=0.20.20
release=1
source=(https://www.musicpd.org/download/mpd/0.20/$name-$version.tar.xz mpd)

build() {
  cd $name-$version

  ./configure \
      --prefix=/usr \
      --sysconfdir=/usr/etc
  
  make
  make DESTDIR=$PKG install

  install -D $SRC/mpd $PKG/etc/rc.d/mpd

  rm -r $PKG/usr/share/doc
}
