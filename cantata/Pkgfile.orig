# Description: Qt5 Graphical MPD Client for Linux, Windows, macOS, Haiku
# URL: https://github.com/CDrummond/cantata
# Maintainer: Tsaop, leeroy at cock dot li
# Depends on:dbus desktop-file-utils expat ffmpeg freetype glib gtk harfbuzz libcddb libcdio libcdio-paranoia libdrm libffi libpng libvdpau libxml2 llvm mesa3d mpg123 qt5 taglib udisks xorg-libx11 xorg-libxau xorg-libxcb xorg-libxdamage xorg-libxdmcp xorg-libxext xorg-libxfixes xorg-libxshmfence xorg-libxxf86vm

name=cantata
version=2.3.1
release=1
source=(https://github.com/CDrummond/$name/archive/v${version}.tar.gz)

build() {
	cd $name-$version
	mkdir build; cd build

	cmake -DCMAKE_INSTALL_PREFIX=/usr \
		-DCMAKE_BUILD_TYPE=Release \
		-DENABLE_LIBVLC=OFF \
		..

	make
	make DESTDIR=$PKG install

	find $PKG \( -name "AUTHORS" -o -name "LICENSE" \) -delete
}
